{
  "name": "sardinefish",
  "version": "1.0.0",
  "description": "",
  "main": "SardineFish.js",
  "scripts": {
    "build": "npm run build:bundle && npm run build:declare",
    "build:bundle": "npx esbuild --bundle --outfile=SardineFish.API.js SardineFish.API.ts",
    "build:declare": "npx tsc"
  },
  "author": "SardineFish",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.11.20",
    "ts-loader": "^8.0.17"
  }
}
